Backend Test - Refactoring code
===

To run the script first install the requirements using a virtual enviroment, then just execute the main file:

    python -m main

To run the tests:

    python -m pytest tests
